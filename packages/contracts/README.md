# Walymarket Contracts (Placeholder)

This is a placeholder package for Sui Move contracts.

If you have the Sui CLI installed, you can re-initialize this package with:

```bash
sui move new contracts
```

Then move your sources into `packages/contracts/sources` and update `Move.toml` accordingly.
